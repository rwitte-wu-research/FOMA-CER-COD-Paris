# BOOTSTRAP — M4 Session (Ch. 2 Theory & Hypotheses) — v2

M-hub authorized, 2026-08-03. **Supersedes the M3-authored v1.0** (committed @ c47c65b); v1.0 content absorbed (mission, carried items, input inventory, phase plan), deltas: canary corrected to the verified real state, six duty additions, governance restated. Ships in the M4 package commit (replaces docs/BOOTSTRAP_M4_SESSION.md). Conversation language: German; artifacts/log entries: English (ERROR_LOG: German).

**Governance (restated — second drift occurred):** bootstrap authorship = **M-hub only**. Task sessions close with a hub sync (commit hash · delivered · open forks) and do **not** author the successor bootstrap; M4's P5 is the sync, nothing else. A recurrence gets its own ERROR entry. Design forks touching the map or frozen DECs escalate to the M-hub **before** commit; wording-level questions are session-local.

**Phase goal:** Chapter 2 per Map §2/§4: **§2.1** CER–COD mechanisms → H1 (frozen wording, Map §3) · **§2.2** Paris → H2 in the two-sided tension architecture (R1: H2 retained directionally, *adjudicated — not advocated*; counter-mechanisms pulled forward from the old draft's discussion) · **§2.3** consolidated moderator expectations for the RQ3 dimensions (featured-depth blocks + collective paragraph, expectation sentences, **no H numbers**; closing sentence per Map §4). Output: `manuscript/ch2_theory.md` + `manuscript/ch2_theory_references.md`.

---

## §1 Canary (MANDATORY; deviation from THIS list = STOP + file request)

1. `docs/DECISION_LOG.md` — last entry **DEC-055** (2026-08-03, M3, FROZEN).
2. `docs/ERROR_LOG.md` — last entry **#43** (2026-08-03, delivery-path class, CLOSED). #41 does not exist yet (reserved call for the bp landing) — conform. If the bp strand landed first, a bp DEC + #41 may precede — also conform, not a STOP.
3. `analysis_plan.md` — last addendum **A.16** (M-sessions do not touch the plan).
4. `docs/CER-COD_Status.xlsx` — **known-incomplete M3 touch (expected state, not a STOP):** DLI A76 = DEC-055 pointer ✓, but **A2 still "71 DECs" (stale)** and **Ablauf G15 still "offen (M3 per DEC-052)"** — repairs are M4 duties (§3.4). Any *other* deviation = STOP.
5. `docs/M_PHASE_MAP.md` = v1.1; §3 carries RQ4 as **DRAFT ("freeze at M4")** — that freeze happens in this session (§3.1).
6. `manuscript/ch1_introduction.md` (with the RQ4 DRAFT comment), `ch3_methods.md`, `ch4_results.md` + ledgers, `docs/novelty_scan_K1.md` present in PK.

Git anchor: c47c65b (M3 package, HEAD at authoring). Self-locating rule applies after the M4 commit.

---

## §2 Mission inputs (absorbed from v1.0, binding)

- **Map v1.1** §1 (vocabulary zones — **§2.2's H2 statement and section close are register-governed**; check the zone list before drafting), §3 (H1/H2 frozen wordings; RQ catalog), §4 (2.3 build plan verbatim: instrument fullest block · CER measurement short · regulation condensed · country/institutional short · collective paragraph incl. industry core in two sentences; directional expectations as prose; closing sentence fixed), §9 (Ch.-2 audit verdict: 2.1 and the 2.3–2.5 theory bodies largely hold; 2.2 is the rebuild).
- **Conventions:** "pre-specified" throughout · CHE–RVE where the model is named · skeleton-v2 section names in any forward references · O-A' (H1+H2 only; no H3+ language anywhere) · "Ch. 1 motivates, Ch. 2 derives" (DEC-055 Q10 — no re-argument of the Ch.-1 gap material, derive from it).
- **Input inventory (v1.0):** old draft = material quarry (architecture new, material reusable — same salvage logic as M3) · COE companion = house template for §2.1–2.3 · Zarea = theory-section contrast foil · Bauer et al. = conceptual reference point · K2a evidence architecture (T1a/T1b spine) for §2.2's two-sided tension.
- **Kleimeier & Viehs double-paper disambiguation** if cited in Ch. 2 (FRL 38:101440 vs. Economics Letters 205:109936; the v12 ledger decides which is coded).

## §3 Duties (all ship in the M4 package)

1. **RQ4 freeze (DEC-053 ripple, scheduled here):** adopt/finalize the Map §3 DRAFT wording ("Is the pooled CER–COD association robust to publication selection?" — adjust only on a drafting-exposed defect); log in the M4 DEC; remove the DRAFT comment in `manuscript/ch1_introduction.md` (one-line edit, listed in the DEC); **Map v1.2 touch:** RQ4 = FROZEN + appendix-docking note ("appendix assembly docked to M6, author confirm at M6 start").
2. **Fig. 1 framework update (Map §7 M4 row, missing in v1.0):** the conceptual framework figure must reflect the final architecture (H1 · H2 with the two-sided tension · RQ3 dimensions · RQ4 as validity layer). **M4-F1 clarification at P1:** generation path — scripted (R/15 for reproducibility, CC-in-Windows-terminal lesson per DEC-054 §4) vs. author-supplied diagram; rule it, log it, place caption manuscript-side per F-CAP.
3. **ERROR log append — the P1 misassumption entry (§7 verbatim; third attempt):** copy from THIS FILE; number = next free at the log; **report the assigned number explicitly in the M4 hub sync.**
4. **Status repairs + touch (three-cell minimum rule, new standing pin):** A2 count → correct total *including* the M4 DEC row · A76ff pointer append for the M4 DEC · Ablauf G15 → "erledigt (M3 …)" retro-repair · G16 → M4 progress note. **Verify by re-read before hashing.**
5. **No workbook edits, no appendix work.** bp strand stays outside (if it lands mid-session: separate package; ch1-a7/ch4/TB-08 slot resolutions belong to that package).
6. **Reference ledger:** `manuscript/ch2_theory_references.md` (ledger policy per DEC-053; [VERIFY] harvests → M7 list).

## §4 Phase plan (v1.0 pattern, P5 corrected)

P1 decision map (Ch.-2 architecture + section budgets + theory-source inventory + open forks M4-Q series, incl. M4-F1) → P2 author rulings → P3 delivery units (draft EN + composition legend + citation manifest + micro-rulings) → P4 chapter freeze (verification battery: H1/H2 frozen-wording 0-diff check, zone greps, "no H3+" grep, "pre-registered" zero-hit grep) → **P5 package + hub sync** (M4 DEC, ledgers, status per §3.4, Map v1.2, this bootstrap v2, ritual incl. `git push origin main`; **no BOOTSTRAP_M5 authorship — the hub authors it after the sync**).

## §5 Carried items (not M4 scope)

**Submission gates:** #16 PRISMA + #23 note (Volker; A-risk) · #37/bp → TB-08 → ch1-a7 + ch4 slots (#41 call at landing) · #33 waiver (M7) · R5 (M7). **M5 carries:** a6-limits → boundary paragraph · explanation-level ripple (post-M5 micro-ruling re a7/a8 mechanism sentence) · TB-55/61/64 homes per Map §5. **Volker parallel:** Bauer-T2 eligibility triage (21+3; list in novelty_scan_K1.md §3; default = defend, amend only per own DEC). **[VERIFY]/M7 ledger:** novelty_scan_K1.md §6 + ch1 ledger footer. **Author scratch in repo root (gedanken.txt, h):** untracked; hub recommendation = move out of the repo or delete before the next commit (author's call; do not add).

---

## §6 Package & ritual

Expected file list (final from git evidence): `manuscript/ch2_theory.md` · `manuscript/ch2_theory_references.md` · `manuscript/ch1_introduction.md` (DRAFT-comment removal) · Fig.-1 artifact (path per M4-F1 ruling) · `docs/M_PHASE_MAP.md` (v1.2) · `docs/DECISION_LOG.md` (M4 DEC, commit-order) · `docs/ERROR_LOG.md` (§3.3 entry) · `docs/CER-COD_Status.xlsx` · `docs/BOOTSTRAP_M4_SESSION.md` (this v2). Ritual: placement table (paths from git evidence — #43 lesson) → certutil B-stage, line-wise → C-block → `git log -1 --stat` paste → `git push origin main` → PK swap → hub sync.

---

## §7 ERROR entry — verbatim text (German; number assigned at the log; report it in the sync)

#NN 2026-08-01 [Claude/M-Hub P1] (Nachtrag — dritter Anlauf: #37/#38 wurden für M1-/M2-Ereignisse vergeben, die M3-Duty §5.3 blieb unausgeführt; der Eintrag reiste dreimal ungeloggt weiter, ab jetzt quellgesichert über docs/BOOTSTRAP_M4_SESSION.md §7) Unverifizierte Repo-Annahme in der P1-Auslieferung (#19-/#32-Klasse) — docs/overlap_disclosure_v12.md wurde in der C-Freigabe („3× untracked") und in der DEC-052-Consequences-Zeile als neue Commit-Datei deklariert; git status bewies die Datei als bereits getrackt und byte-identisch (Einzug bereits mit Hub-Commit 890242c; R7 damit faktisch vorvollzogen); Wirkung: null — Commit eb0866f vollständig und korrekt mit 4 Dateien, die DEC-052-Consequences-Zeile nennt 5 (gegen diesen Eintrag zu lesen); Beitragsfaktor [Autor]: Commit trotz vom angekündigten Statusbild abweichendem git status ohne Stopp ausgeführt; Lehre: Untracked-/Neu-Datei-Erwartungen sind wie Pfade aus git-Evidenz abzuleiten (git status/ls-files), nie aus der Lieferlogik, und jede Statusabweichung sperrt den C-Block bis zur Klärung; GESCHLOSSEN mit dem M4-Commit [DEC-Nummer bei Paketbau einsetzen]
