# T5 — Publication-bias battery (Block D: D1/D2/D4/D5 + D6 draft) · manuscript feed

Language per the resolved gate [DEC-044 → DEC-047]. Source of record:
`output/T5_results.csv` @ commit 60756bb (run 2026-07-19 13:37 CEST; run basis 6bf40b1; verifier
**27/27 PASS**; 53 rows; dat_prep md5 `6702ef3dc45fe0b693b13f50ebd1576b`; seed 20260710). Text blocks
**TB-30…TB-35** live in `output/T5_results_workbook.xlsx` › Manuscript_Text_Blocks (numbering continues
from T8 = TB-28; **TB-29 was never assigned** — documented gap, append-only, no renumbering); formula-live
numbers in Manuscript_Inputs. **D3 (RoBMA) does not run here** — it executes in the H-session as H2 [DEC-031g].

## Battery summary (D1/D2/D5 on the estimation set 2,713 ES / 115 studies / 114 clusters [DEC-042a]; D4 on the period cells pre 1,994/83/82 · post 711/31/31 [DEC-042b])

| Move | Model | Key result | Read-out |
|---|---|---|---|
| D1 · FAT-PET-PEESE | `zi ~ sez` and `zi ~ I(sez²)` on the 3L-RVE spine, CR2/Satterthwaite | FAT slope −0.642 (p = .0500; CI edge +0.0001); PET intercept −0.043 [−0.076; −0.010] (p = .011, df 100.3); PEESE intercept −0.055 [−0.084; −0.027] (p = .0002, df 109.9) | rule fires on PET → PEESE carries the corrected estimate; corrected mean smaller than unadjusted, sign retained |
| D2 · Selection models | 3PSM (stepfun, step .025, `alternative = "less"`) + p-uniform* (`side = "left"`) on 114 one-per-cluster aggregates | 3PSM μ = +0.012 [−0.071; +0.094] (p = .781), δ = 0.49, LRT χ²(1) = 5.05, p = .025; p-uniform* μ = +0.012 [−0.069; +0.098] (p = .778); unadjusted ML −0.062 [−0.109; −0.014]; τ² ≈ .066–.069 across models | selection-adjusted means at ≈ 0 with CIs spanning zero; the LRT registers selection against nonsignificant results |
| D4 · Pre/post-Paris split | identical machinery and pins per period; upgrade FIRED [DEC-031a.5] | pre: FAT p = .162; PET −0.046 (p = .0497) → PEESE −0.058 [−0.098; −0.018]; 3PSM −0.022, δ = 0.69, LRT p = .309. post: FAT −1.238 (p = .0047); PET −0.025 (p = .0098) → PEESE −0.045 [−0.061; −0.028]; 3PSM −0.006, δ = 0.34, LRT p = .080 | rule selects PEESE in both cells; asymmetry evidence sits in the post cell; no pre-vs-post test is pre-registered |
| D5 · Grey-literature panel | `zi ~ 0 + q_status` cell-means on the spine, CR2, contrast (−1, +1) + HTZ Wald | published −0.059 [−0.090; −0.028] (2,033/101/100) vs not published −0.057 [−0.104; −0.009] (680/14/14); contrast **+0.002** [−0.054; +0.059], t(17.1), p = .928; HTZ F = 0.008 | direct, retrieval-conditional selection check: precise null |
| D6 · Triangulation | cross-method convergence table | FINAL; regression-corrected −0.04 to −0.06 z vs selection-corrected ≈ 0; all corrected point estimates inside the primary SESOI band (\|r\| = 0.070) | RoBMA row delivered by TH-b (H2a, single home); final line per the resolved gate [DEC-047] — TB-35 READY |

T1/A5 identity anchor [DEC-031g/F65]: REML/KnHa on the same 114 aggregates = −0.0617 [−0.1099; −0.0135],
df 113, p = .0126 — value-located in committed `output/T1_results.csv`, hard-checked at 1e-9 against
embedded full-precision constants (CSV-drift canary) and independently re-fitted at 1e-6 (verifier O13–O15).

## Disclosures carried into the text

- FAT boundary (full set): the two-sided CR2/Satterthwaite p rounds to .0500 with the CI upper bound at
  +0.0001. No dichotomous asymmetry claim is made; the alignment rule conditions on the PET intercept
  (p = .011), never on FAT [DEC-010; P-T5-1].
- Knife-edge rule firing in the pre cell: PET p = .049729, i.e. the rule fires by 0.0003. Both PET and PEESE
  are computed and reported in every cell regardless — the knife-edge is disclosed, not resolved by relabeling.
- Realized Satterthwaite df on the quadratic PEESE slopes is small (full 6.4 · pre 5.0 · post 2.0); slope rows
  are diagnostic. The claim cells are the intercepts (df 109.9 · 79.3 · 25.4).
- Selection-model rows carry z-tests without df by schema convention: `selmodel`/`puni_star` inference is
  Wald-z / likelihood-based at the aggregate level, so cluster-robust df do not apply.
- D5 conditions on retrieval: the panel compares retrieved published against retrieved grey studies, not
  against unretrieved ones; the model-based estimators (D1/D2) address the latter. The two facts are not in
  mechanical conflict — 3PSM models selection at the significance threshold irrespective of outlet.
- No formal pre-vs-post test of bias parameters is pre-registered; D4 is an appendix disclosure panel
  [plan §8/A.12]. Period differences are described, not tested.
- Divergence between the regression-based (D1) and selection-based (D2) corrections is an expected,
  documented estimator property (Carter et al. 2019); the joint reading is D6's, finalized per DEC-047 after TH-b.
- Direction pins asserted jointly [P-T5-2]: `selmodel(alternative = "less")` ≡ `puni_star(side = "left")`
  (selection favors significant negative effects). `sez ≡ sqrt(vi)` derived in-script; the raw v12 SE column
  is never read; the PEESE regressor `I(sez²)` ≡ `vi`.
- Post-cell selection models run under the fired conditional upgrade [DEC-031a.5] (31 studies ≥ the
  20-study threshold; k = 31 aggregates). Wide CIs are expected and shown.
- q_status cell inventory pinned ex ante and result-blind (F60 pattern): 2,033/101/100 vs 680/14/14; both
  cells clear the 3-cluster inference floor; the single multi-study cluster lies inside the published cell.
- Convergence: 7 certificates, all on ladder rung 1/3 (nlminb, no fallbacks); R6 control re-fit under
  optim/BFGS max\|Δβ\| = 5.665e-06 [DEC-031f]. Known warning: clubSandwich deprecation of
  `impute_covariance_matrix()` on the identical committed T1/T8 spine call — on-demand register candidate,
  no action in T5.
- Absences asserted by the verifier: no D3/RoBMA rows (→ TH-b), no bp rows [P-T5-5], no trim-and-fill
  [DEC-031a.4], no `not_estimable` rows in this run.
- Single-home: the pre/post-Paris bias split reports from D4/T5 only; the RoBMA line reports from TH-b; the
  T1/A5 anchor is a verification row, not a second reporting home for the pooled estimate.
- Gate discipline [DEC-044 → DEC-047]: T5 supplied the third Pending-C trigger element (PET); the language
  gate resolved 2026-07-27 [DEC-047]. Workbook status: TB-30…TB-35 READY — TB-35 finalized and the pre-gate
  "Pending-C" status strings updated in the post-gate sync (2026-07-29).

## Verdict line (gate input — no framing)

Every corrected pooled estimate is smaller in magnitude than the unadjusted mean, and the corrections split
by estimator family: regression-based corrections retain a small negative mean (PET −0.043, PEESE −0.055 z),
selection-based corrections move to approximately zero with CIs spanning it (3PSM +0.012 ≍ p-uniform*
+0.012), the 3PSM likelihood-ratio test registers selection against nonsignificant results (p = .025), and
the direct grey-literature contrast is a precise null (+0.002, p = .928). All corrected point estimates lie
inside the primary SESOI band (|r| = 0.070). Within periods the same mechanical rule selects PEESE in both
cells, with the funnel asymmetry concentrated in the post cell and untested against the pre cell by design.
The joint characterization is the D6 triangulation line, finalized after TH-b supplied the RoBMA row;
wording per the resolved gate [DEC-031g; DEC-044 → DEC-047].
